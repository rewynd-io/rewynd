type: object
description: "Ascending for the next episode, Descending for the previous episode."
properties:
  episodeId:
    $ref: "./EpisodeId.yaml"
  sortOrder:
    $ref: "../util/SortOrder.yaml"
required:
  - episodeId
  - sortOrder
