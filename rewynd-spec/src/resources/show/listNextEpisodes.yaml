post:
  operationId: listNextEpisodes
  security:
    - CookieAuth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/show/ListNextEpisodesRequest.yaml"
  responses:
    "200":
      description: "Successful query."
      content:
        application/json:
          schema:
            $ref: "../../schemas/show/ListNextEpisodesResponse.yaml"
