post:
  operationId: listEpisodesByLastUpdated
  security:
    - CookieAuth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../schemas/show/ListEpisodesByLastUpdatedRequest.yaml"
  responses:
    "200":
      description: "Successful query."
      content:
        application/json:
          schema:
            $ref: "../../schemas/show/ListEpisodesByLastUpdatedResponse.yaml"
